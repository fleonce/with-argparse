config = {
    #
    # whether the argparse functionality is disabled at all!
    #   enabled by default
    "argparse_enabled": True,
    #
    # add a help flag aka -h to the argparse object?
    #   enabled by default
    "add_help": True,
}